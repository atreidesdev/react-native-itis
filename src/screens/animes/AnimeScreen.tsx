import React from 'react';
import { View } from 'react-native';
import AnimeList from '../../entities/anime/ui/AnimeList.tsx';

const AnimeScreen = () => {
    return (
        <View>
            <AnimeList />
        </View>
    );
};

export default AnimeScreen;
